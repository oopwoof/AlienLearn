# AlienLearn · 一页纸

**一款 LLM-Agent 驱动的沉浸式外语语境沙盒。** 玩家扮演潜伏地球的外星人，接入一家雨夜拉面馆，
用目标语言点一碗面、套出老板不写在菜单上的高汤配方。说错话不会弹出红叉 —— **世界会开始裂开。**

| | |
| --- | --- |
| 我的角色 | 独立完成：产品定义、Prompt 与评测设计、全栈实现 |
| 周期 | 5 天 MVP + 两轮带评测的迭代 |
| 技术栈 | FastAPI · SSE 流式 · 原生 JS 零构建 · DeepSeek（OpenAI 兼容，换厂商只改 `.env`） |
| 北极星指标 | **单局主动输出的目标语言词数**（不是学习时长） |
| 代码与四份评测报告 | github.com/oopwoof/AlienLearn |

## 问题与洞察

语言学习的真问题不是内容不够，是**不敢开口**和**没有语境**。
而弹窗式纠错让人越纠越沉默 —— 每一个红色提示都在提醒你"你在考试"。

洞察：**把纠错从"评判"改成"世界的反馈"。** 老板绝不解释语法，只是皱眉、反问、
或按自己的理解上菜；精确诊断静默滑进侧边栏，玩家可以不看。

## 核心机制：色差分离

视口把场景渲染成红、青两个色彩通道再叠加。伪装稳时两层重合、画面清晰；
暴露度升高，两层沿 X 轴分离。**视觉惩罚不是贴上去的特效，它就是渲染管线本身。**

## 架构与三个刻意的取舍

`Router →（Pedagogy ∥ Persona）→ 纯 Python 状态机`

- **没上 LangGraph**：三个节点上编排框架是过度设计。我要的是控制权 —— Router 必须先行，
  Pedagogy 与 Persona 必须并行（实测首字 1.43s；串行玩家要多等一整个 JSON 才能看到老板开口）
- **数值一律不给 LLM 碰**：伪装度、能量、任务阶段全在 Python 里，LLM 只发信号。
  防幻觉，也防玩家用 prompt 注入把血条改满
- **词数由代码算**：指标口径一旦被幻觉污染，后面所有基于它的判断都不可信。
  越狱也是一串英文，但不是学习行为，不计入北极星

## 四轮迭代（每轮都有留档报告）

| | 对局链路 | Router 拦截率 | Pedagogy 诊断准确率 |
| --- | --- | --- | --- |
| 一 | 规则桩 | 83.3% —— 低于我自己在 PRD 里写下的 90% 判据 | — |
| 二 | 规则桩 | 100% | 100%（不可信，见下） |
| 三 | DeepSeek | 100% | **73.3%** |
| 四 | DeepSeek | 100% | dev 集 100% / **留出集 70%** |

**第四轮是我最想讲的一轮。** 看到 73.3% 我的第一反应是"模型不行"，翻完原始 trace 后结论是反的：
模型每一条诊断都对（`I is`→`I am`、`want eat`→`want to eat`，span/改法/说明无一处错），
它只是把档位判成了 minor —— 而按我 prompt 里亲手写下的 `major = 影响理解`，**它是对的**。
是我的人工标注偷偷换了另一把尺子。**这是标注一致性问题，不是模型能力问题。**

所以我改的是判据不是模型：严重度改量**「暴露度」**而不是「可理解度」——
它该回答的是"母语者会觉得这人口音重，还是觉得这人不对劲"。
**30 条人工标注一条没动**，改标注凑分数是移动球门。

dev 集因此升到 100%，但它的句子被我写成了 prompt 的 few-shot 锚点，满分是保送的。
所以我另补了 10 条留出集：**它只拿到 70%，而我刻意没去刷它** ——
留出集一旦用来调 prompt 就作废。下一步是换手段（把档位判定挪进代码结构规则），
不是往 prompt 里堆例子。

## 边界

**有意不做**：账号、支付、公网部署、UGC 编辑器、语音、微调。
它们在 PRD 里都有位置，但不验证 MVP 要验证的那个假设 ——
**"把纠错藏进世界规则里，玩家会不会更愿意开口"**。

**最大的短板**：留存尚未验证。靠新奇感驱动的机制会衰减，这点我拿不出数据，
需要真实用户测试 —— 所以报告里它写的是"待验证假设"，不是结论。

---

想看更深：[README](../README.md) · [面试讲述工具箱](interview-kit.md) · [录屏脚本](demo-script.md) · [评测证据](../eval/out/)
